[pytest]
testpaths = backend hardware/tests
python_files = test_*.py
markers =
    network: 실 외부 API(광주 BIS, 기상청 등) 호출이 필요한 통합 테스트. mock 테스트만 돌리려면 `pytest -m "not network"`.
filterwarnings =
    ignore::DeprecationWarning:flask_limiter.*
    ignore::DeprecationWarning:limits.*
