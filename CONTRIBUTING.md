# BUSTAGO 팀 기여 규칙 및 AI 산출물 검토 프로세스

> 버전: v1.0 | 작성일: 2026-05-01

---

## 1. AI 사용 원칙

> **"AI가 시키는 게 아니라, 우리가 AI를 시키는 것"**

- AI는 도구다. 생성된 결과물의 책임은 사용한 팀원에게 있다.
- AI 결과물을 그대로 복사·붙여넣기 금지. 반드시 검토 후 사용.
- "AI가 이렇게 말했다"는 보고 금지. 우리가 확인한 사실만 보고.

---

## 2. AI 산출물 제출 전 필수 검토 체크리스트

Git에 커밋하거나 제출하기 전 아래 항목 모두 확인:

```
[ ] 내용을 본인이 완전히 이해했는가
[ ] 프로젝트 실제 상태(코드, DB, API)와 일치하는가
[ ] 추정/가정이 사실처럼 기술되지 않았는가
[ ] 수치(정확도, FPS, 예산 등)가 실제 측정값과 동일한가
[ ] Git에 커밋하기 전 팀장 또는 담당자가 확인했는가
```

위 항목 중 하나라도 "아니오"이면 커밋하지 말고 수정 후 재확인.

---

## 3. Git 업로드 규칙

### 커밋 메시지 형식

```
<type>: <제목>

예시:
feat: route-recommend API 추가
fix: API_BASE localhost → window.location.origin 수정
docs: 하드웨어 시스템 개념도 작성
chore: 구버전 문서 삭제 (v2~v4)
```

**type 목록:**
- `feat`: 새로운 기능
- `fix`: 버그 수정
- `docs`: 문서 작성/수정
- `chore`: 기타 (삭제, 정리 등)
- `test`: 테스트 추가/수정

### 업로드 의무

- **개인 조사 결과도 반드시 Git에 업로드.** 로컬에만 보관 금지.
- 회의 전날까지 관련 내용 push 완료.
- 커밋 단위: 논리적으로 하나의 작업 단위. 여러 파일을 묶어도 되지만 무관한 작업 혼합 금지.

---

## 4. 보고 규칙

### 금지 사항

- ❌ "조사 중입니다" (진행률 없는 보고 금지)
- ❌ "확인해보겠습니다" (언제까지 확인할지 명시 없는 보고 금지)
- ❌ "아마 ~일 것 같습니다" (추정 보고 금지)

### 올바른 보고 형식

```
✅ "[항목명] 60% 완료, [날짜]까지 완료 예정"
✅ "[사실 내용] — [출처/확인 방법]으로 확인"
✅ "[문제 상황]: [원인], [해결 방안] 제안"
```

---

## 5. 브랜치 전략

```
main          ← 완성된 기능만 merge (팀장 승인 필요)
feat/hunmin   ← 류훈민 작업 브랜치
feat/[이름]   ← 팀원별 작업 브랜치
```

- 개인 브랜치에서 작업 후 팀장에게 merge 요청
- main에 직접 push 금지

---

## 6. 문서 작성 규칙

1. **출처 명기:** 외부 데이터, API 명세, 논문 등은 반드시 출처 기재
2. **추정 표기:** 확인되지 않은 내용은 `(확인 필요)` 또는 `(추정)` 명시
3. **버전 관리:** 문서 수정 시 상단 버전 번호 갱신 (v1.0 → v1.1)
4. **구버전 삭제:** 새 버전 작성 완료 후 구버전 파일 삭제 (Git에서도)
5. **날짜 형식:** `YYYY-MM-DD` 통일

---

## 7. 문의

팀장: 박건우  
AI/Backend: 류훈민  
Frontend/HW: 이건영  
Backend/DB: 이트겔
