# BUSTAGO 프로젝트 종합 진단

- **작성일:** 2026-05-16
- **관점:** Martin Fowler (Refactoring, Evolutionary Architecture) + Robert C. Martin (Clean Architecture, SOLID)
- **목적:** 현재 프로젝트 구조·코드 품질·실행 가능성·유지보수성·리스크의 솔직한 평가
- **방식:** 파일 변경 없는 읽기 전용 조사 (외부 산출물·감리와 별개의 독립 평가)

---

## 1. 한 줄 총평

> **광주대 캡스톤 졸업작품 기준으로는 "MVP 가능, 시연 준비된 작품"이지만, 운영 시스템 관점에서는 보안 1건·ML 데이터 1건·예외처리 다발의 누적된 부채가 있고, CI 부재로 회귀를 잡아낼 안전망이 없는 상태다.**

---

## 2. 프로젝트가 하는 일

**BUSTAGO** — 광주대학교 셔틀·시내버스 정류장의 **혼잡도 예측 + 실시간 카운팅** 시스템.

- 데이터: 서울시 공공 API(승하차·차내혼잡도·기상)로 학습 모델 구축 → 광주 정류장에 직접 적용
- 추론: ML 모델(RandomForest)이 시간·요일·날씨·이전승차량으로 4단계 혼잡도 분류
- AI 카운팅: Jetson Orin Nano에서 YOLOv11+DeepSORT로 라인 크로싱 실측, Flask 백엔드로 POST
- 사용자: 학생 모바일 PWA(혼잡도+노선추천+버스도착) + 운영자 PC 대시보드(실시간 카운팅)
- 데모: 2026-05-21 1차 / 5-28 교내 경진대회 / 6-04 최종

---

## 3. 구조 요약

| 디렉토리/파일 | 역할 | 규모/상태 |
|---|---|---|
| `backend/app.py` | Flask 엔트리, 5개 Blueprint 등록, 에러 핸들러 | 69줄, 깔끔 |
| `backend/routes/` | predict / stats / stations / crowd / recommend | 750줄 합계 |
| `backend/models/db.py` | MySQL/SQLite 듀얼 드라이버, fallback | 150줄, 보조 추상화 |
| `backend/test_app.py` | pytest 14개 (health, predict, crowd, route, GJ BIS, stations) | 134줄, 수집 OK |
| `backend/schema.sql` | 5 테이블 (stations, predictions, weather_cache, crowd_counts, routes) | 85줄, 시드 데이터 포함 |
| `ml/data_collection/` | 서울시 API + 기상청 수집기 4종 + 통합 pipeline | asyncio.gather 적용 |
| `ml/preprocessing/build_features.py` | 7-Feature DataFrame 구축 | **weekday=placeholder 문제** |
| `ml/models/train_rf.py` | RandomForest 학습 (n=10) | 모델파일 `rf_model.pkl` 225KB 존재 |
| `ml/models/train_lgbm.py` | LightGBM 학습 인프라 | **모델 파일 미생성 (코드만)** |
| `ml/models/predict.py` | 추론 인터페이스 (lgbm 우선, rf fallback) | 175줄 |
| `hardware/counter.py` | YOLOv11+DeepSORT+Line Crossing | 352줄, 2026-05-16 35 FPS 검증 완료 |
| `frontend/student/app.js` | PWA 학생 화면 | 326줄, var/ES5 |
| `frontend/admin/dashboard.js` | 운영자 실시간 카운팅 패널 | 474줄 |
| `frontend/shared/api.js` | API wrapper (`window.location.origin`) | 57줄 |
| `docker-compose.yml` | backend + frontend, MySQL 없음 | 35줄 |
| `data/` | 학습 데이터 (서울 boarding 6개월 + weather + congestion) | 5MB CSV 실재 |
| `.github/workflows/` | **부재** | CI/CD 없음 |
| `docs/` | 한국어 산출물 (계획·설계·구축·테스트·배포·조사 + 회의준비) | 매우 잘 정리 |

---

## 4. 기술 스택

| 레이어 | 기술 | 근거 |
|---|---|---|
| Backend | Python 3.10 + Flask 3.0 + flask-cors + Flask-Limiter | `backend/requirements.txt:1-3` |
| DB | MySQL 우선 / SQLite 폴백 (pymysql) | `backend/models/db.py:23-86` |
| ML | scikit-learn RandomForest (운영) / LightGBM (개발) | `ml/models/train_rf.py`, `train_lgbm.py` |
| AI 비전 | YOLOv11 (ultralytics) + DeepSORT + OpenCV | `hardware/counter.py:31-37` |
| 하드웨어 | Jetson Orin Nano (JetPack 6.2) | `docs/03_구축/Jetson_SW스택_구축_2026-05-16.md` |
| Frontend | Vanilla JS (ES5) + PWA + Service Worker + Leaflet/Chart.js | `frontend/student/service-worker.js` |
| 외부 API | 광주광역시 BIS, 기상청 초단기실황, 서울 열린데이터 | `backend/config.py:30-41` |
| 배포 | Docker Compose 2-service + Gunicorn + Nginx | `docker-compose.yml`, `backend/Dockerfile` |
| 테스트 | pytest | `backend/test_app.py` (14건) |
| CI/CD | **없음** | `.github/` 부재 확인 |

---

## 5. 좋은 점

1. **Flask Blueprint 분리가 깔끔**. routes 5개가 각자 한 가지 책임만 가짐. `app.py`에서 단순히 register만. 단일 책임 원칙 OK.
2. **인풋 검증 일관성**. 모든 라우트에 `re.fullmatch(r"[A-Za-z0-9]{3,10}")` 같은 정규식 가드. SQL injection 표면 좁힘.
3. **캐시 계층 의식**: predict.py에 TTL 300초 자체 캐시, stats.py에 TTL 60초 캐시, weather_cache 테이블. ML 추론 비용을 의식적으로 줄였음.
4. **DB 듀얼 드라이버 + 자동 fallback**: 개발 환경(SQLite)과 운영(MySQL)을 한 번에 지원. 의도 자체는 좋음.
5. **Rate Limiter 도입**: 200 req/day, 60/min 기본 + 라우트별 세부 제한. 합리적 데모 가드.
6. **에러 응답 일관성**: 전역 errorhandler 400/404/429/500 모두 같은 JSON 스키마. 프론트가 다루기 쉬움.
7. **문서 정합성**: `docs/`가 매우 체계적이고 (계획→설계→구축→테스트→배포→조사 6단계 + 회의준비), 외부 감리 보고서까지 받아 P0~P3 정리되어 있음. 캡스톤 수준 이상.

---

## 6. 걱정되는 점

### 6.1 보안 (Critical)

🔴 **`backend/config.py:38-40` — GJ_BIS API 키가 코드에 하드코딩**:
```python
GJ_BIS_API_KEY = os.getenv("GJ_BIS_API_KEY", "bN25YqJDY...HQ%3D%3D")
```
`.env`가 없어도 동작하도록 default 값에 **실제 키가 박혀 있음**. 이게 public repo이면 즉시 키 무효화 대상. 외부 감리에서도 P0-1으로 지적됐고, 사용자가 "시연 직전 키 교체 위험" 때문에 미루는 결정을 한 상태 (회의 안건 3 deferred). 이 결정 자체는 risk-managed 선택이지만 **시연 끝나면 무조건 처리 + 키 rotate** 필요.

🔴 **`.env` 파일 디스크에 존재**: `ls -la`에서 `.env` 235바이트 보임. `.gitignore`에는 들어있지만, **과거 커밋 히스토리에 잔존 가능성** 있음. `git log --all --full-history -- .env` 로 확인 권장.

### 6.2 ML 정확도 (High)

🟡 **`ml/preprocessing/build_features.py:110` — weekday가 placeholder**:
```python
df["weekday"] = df["hour"].apply(lambda h: 0 if h < 12 else 2)  # placeholder
```
"hour < 12이면 월요일, 아니면 수요일"이라는 의미 없는 매핑. **7개 feature 중 1개가 정보 0bit**. 모델 정확도 ML 보고서 90%는 이 상태에서 측정됨 → **실제 weekday로 재학습 시 성능이 어떻게 바뀔지 모름**.

🟡 **RandomForest n_estimators=10**: 트리 10개는 매우 작음. 일반적 권장은 100~500. 합성 500행 기준 0.90 accuracy인데, 1) 데이터 작음 2) 모델 작음 3) feature 1개 noise → 과적합 의심.

🟡 **광주 데이터 부재**: 학습 데이터 전체가 서울 승하차 데이터. README에도 "광주 환경에 직접 적용"이라 명시하고 도메인 이동 위험을 안고 있음. 본인들도 인지 중이고 광주 현장 수집을 후속 작업으로 잡고 있긴 함.

### 6.3 에러 처리 (Medium)

🟡 **`except Exception: pass` 다발**:
- `predict.py:156-157` DB INSERT 실패 → silent
- `crowd.py:58-59` DB INSERT 실패 → 500만 반환, 로그 X
- `recommend.py:91-92` 날씨 조회 실패 → silent
- `routes/stations.py:86-87` 외부 API 실패 → silent
- `routes/stations.py:114-116` 광주 BIS 호출 실패 → `print` (로깅 X)

이런 패턴이 6곳 이상. 시연용으로는 "끊김 없음"이라 좋아 보이지만, **버그가 사일런트하게 살아 있다가 발표 중 갑자기 나타날 수 있음**. 최소한 `logging.exception(...)`로 흔적은 남겨야 함.

### 6.4 중복 (Medium)

🟡 **데이터 단일 출처(Single Source of Truth) 깨짐**:
- `backend/routes/recommend.py:16-33`의 `STATIC_ROUTES` dict
- `backend/schema.sql:76-85`의 routes 테이블 INSERT
- 두 곳에 같은 419번/518번/SHUTTLE 데이터가 따로 적혀 있음. 한쪽 수정 시 다른 쪽 동기화 책임이 사람에게 떨어짐. **DRY 위반 + 운영 중 데이터 어긋남 위험.**

### 6.5 테스트 (Medium)

🟡 **`assert resp.status_code in (200, 502)`** (test_app.py:113, 119):
외부 광주 BIS API 의존하는 테스트가 "200 또는 502 둘 다 OK"로 통과 기준이 느슨. 네트워크 없으면 502 떨어져서 그냥 통과. **실제 검증력 없는 테스트**. 차라리 mock으로 두 케이스를 분리해야.

🟡 **테스트 격리 부족**: `test_crowd_count_get_success` 와 `test_crowd_count_get_no_data` 가 같은 SQLite 파일 공유. CI에서 순서 의존 깨질 수 있음. pytest fixture에서 임시 DB로 격리 필요.

### 6.6 운영 준비 (Medium)

🟡 **CI/CD 없음**: `.github/workflows/` 부재. PR/머지마다 자동 검증되는 게 없음. CONTRIBUTING.md의 "팀장 승인" 수동 의존. 캡스톤이라 OK지만, 데모 후 유지보수 인계 시 첫 1주일 내 회귀가 거의 확실.

🟡 **`docker-compose.yml`에 DB 서비스 없음**: backend 컨테이너가 MySQL 시도 → 실패 → SQLite fallback. "운영 가능" 인척 보이지만 실제로는 단일 디스크 SQLite에 의존. 다중 인스턴스 배포 불가능.

🟡 **DB 어댑터 누수**: `db.py:108-112` `sql.replace("?", "%s")`는 SQL 안의 텍스트 데이터에 `?`가 있으면 깨짐. 예: 사용자 입력이 `"hello?"` 같은 문자열을 SQL에 commit하면 의도치 않게 placeholder로 해석. 현재 라우트들이 모두 정규식으로 입력 막아서 우연히 안 터지는 상태.

---

## 7. 완성도 판단

### **MVP 가능 (시연 가능, 운영 직전 단계 아님)**

**근거:**
- ✅ 핵심 기능 5개 모두 동작 (정류장 조회, 예측, 카운팅 POST/GET, 도착정보, 노선추천)
- ✅ pytest 14개 수집 가능, 그 중 외부 의존 2개 제외 12개는 결정론적
- ✅ Docker Compose로 두 서비스 같이 띄움
- ✅ AI 카운팅 2026-05-16 35 FPS 실측 완료
- ✅ PWA가 실제 광주 BIS API와 통신
- ❌ **운영 가능**으로 못 가는 결정적 이유: API 키 코드 내장 + ML feature placeholder + silent except + CI 부재. 시연용으로는 모두 우회 가능하지만 운영 가면 1주일 안에 사고.

캡스톤 작품 평가 관점이면 **상위권**. 산업용 SaaS 관점이면 **MVP 직전**.

---

## 8. 우선순위 개선안

### P0 — 시연 직후 즉시 (1주일 내)
1. **API 키 하드코딩 제거** (`config.py:38-40`) → `.env` 강제 + 키 rotate
2. **git 히스토리에서 `.env` 잔존 확인**: `git log --all --full-history -- .env`. 있으면 BFG / filter-repo로 제거
3. **`build_features.py:110` weekday placeholder 수정** → 원본 데이터에 실제 날짜 컬럼이 있는지 재조사 후 정상화. 안 되면 weekday feature를 모델에서 빼는 게 솔직함.

### P1 — 경진대회 후 (2주 내)
4. **GitHub Actions CI 1개**: `pytest backend/test_app.py` + `flake8` + `pip-audit`. 한 파일 50줄로 충분.
5. **`STATIC_ROUTES` ↔ `routes` 테이블 단일화**: `STATIC_ROUTES`를 schema.sql 시드만 남기고 코드에서 제거, DB만 truth source.
6. **silent except 6곳 → `logging.exception(...)` 추가** (제어 흐름은 유지, 로그만 남김)
7. **광주 현장 데이터로 ML 재학습**: 7 feature 중 weekday 정상화 후 n_estimators=100 RF 재학습 + 광주 실측 라벨로 검증.

### P2 — 인계/유지보수 단계 (1개월 내)
8. **db.py `?` → `%s` 변환을 PEP 249 paramstyle 추상화로 교체** (named params)
9. **테스트 격리**: 임시 SQLite 파일 fixture (`tmp_path`)로 테스트 간 누수 차단
10. **외부 API mock**: 광주 BIS / 기상청 테스트를 `responses` 라이브러리로 mock — 결정론적 통과 기준 명확화
11. **Docker Compose에 MySQL 서비스 추가** + healthcheck + named volume
12. **PWA E2E 테스트**: Playwright로 학생 화면 골든패스 1개라도 자동화

---

## 9. 내가 이 프로젝트를 맡는다면

1. **`build_features.py:108-110` weekday 문제부터 본다.** 학습된 모델 정확도 90%가 의미 있는 숫자인지 confirm. 원본 boarding CSV에 `use_month` 외에 일자 정보가 없다면 weekday는 모델에서 빼야 함.
2. **GitHub Actions로 pytest를 매 push에 돌린다.** 첫 회 실패하면 그게 진짜 회귀 시작점. 50줄 yaml 한 번이면 됨.
3. **API key fallback 제거 + git 히스토리 정리.** 시연 후 첫 PR.
4. **silent except 일괄 grep → `logging.exception`로 치환.** 30분 작업. 다음 주에 발생할 디버깅 시간 몇 시간 절약.
5. **광주 현장 데이터 수집 일정을 ML과 동기화.** "어떤 데이터를 / 얼마나 / 언제까지 모으면 모델을 어떻게 다시 학습할 건지"의 결정 트리를 문서화. 지금은 "광주 데이터 확보 후"라는 모호한 트리거에 멈춰 있음.

---

## 10. 추가로 확인해야 할 질문

1. **GJ_BIS API 키는 본인 발급 키인가 / 임시 데모용인가?** 본인 키라면 rotate 즉시 필요. 데모용이면 발급 출처를 README에 명시.
2. **광주 boarding 데이터를 어떻게 / 언제 수집할 계획인가?** 광주대 학사팀에서 받는지, 박건우 부팀장의 수동 카운팅인지. 양과 시점이 확정 안 되면 ML 재학습이 계속 지연.
3. **시연 이후 유지보수 책임자가 정해져 있는가?** 졸업 후 1년 운영 가능한 사람이 있는지. 없으면 P1/P2 우선순위가 무의미.
4. **`docker-compose.yml`에 MySQL이 없는 게 의도된 결정인가?** 운영 시 정말 SQLite로 갈 건지, 아니면 별도 RDS/MySQL을 띄울 건지.
5. **합성 데이터 90% accuracy를 발표 자료에 그대로 쓸 건가?** 만약 그렇다면 "합성 데이터 기준" 단서를 반드시 명시하지 않으면 심사위원이 질문하는 즉시 신뢰도 손상.
6. **위 P0 3건을 6/04 경진대회 전에 처리할 시간/의지가 있는가?** 1·3번은 ML 정확도 신뢰성 직결, 발표 Q&A 대응의 핵심.

---

## 평가의 한계 (검증하지 못한 부분)

- 실제 pytest 14개 PASS 여부 — `--collect-only`만 실행, 본 실행 안 함 (네트워크/DB 부수 효과 회피)
- 합성 데이터 외 광주 현장 실측 정확도 — 데이터 자체가 아직 없음
- Frontend admin 대시보드의 실제 렌더링 — 코드만 봤고 브라우저 검증 X
- Docker Compose up — 실행 안 했음 (시스템 변경 회피)
- git history에 `.env` 잔존 여부 — 별도 확인 필요
