flask>=3.0
flask-cors>=4.0
Flask-Limiter>=3.5
pymysql>=1.1
python-dotenv>=1.0
joblib>=1.3
pandas>=2.0
numpy>=1.24
requests>=2.31
scikit-learn>=1.0
gunicorn>=21.0  # WSGI 서버 — backend/Dockerfile CMD에서 사용 (재현성 위해 명시)

pytest>=8.0
responses>=0.25  # HTTP mock — backend integration tests (진단 §6 P1, 2026-05-16)
