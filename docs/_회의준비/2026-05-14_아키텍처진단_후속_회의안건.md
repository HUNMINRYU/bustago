# 회의 안건 — 아키텍처 진단 후속

> **작성일:** 2026-05-14  
> **회의 목적:** `docs/아키텍처_종합진단_2026-05-13.md` 진단서의 보류 항목들에 대한 팀 결정  
> **시연까지:** D-7 (5/21)  
> **기 적용 분량:** P0 6건 중 4건 (변수명 정합화·routes UNIQUE·recommend.py 인덱싱·Docker healthcheck) — 커밋 `c9c7efa`

---

## 안건 1. P0-4 — 모델 파일 부재 시 더미 fallback 정책

### 현황
- `backend/routes/predict.py`는 ML 모델 파일이 없거나 로딩 실패 시 try/except로 가짜 응답을 200 OK로 반환.
- 진단서 §6.4: "시연에는 편하지만 운영에서는 장애를 숨긴다."

### 옵션
| 옵션 | 동작 | 시연 영향 | 운영 영향 |
|------|------|----------|----------|
| A. 현 상태 유지 | 모델 없어도 가짜 응답 | 데모 안전 | 장애 은닉 |
| B. 즉시 에러 노출 | 500 응답 | **시연 중단 위험** | 장애 가시화 |
| C. `DEMO_MODE` env 토글 | 환경변수로 분기 | 데모 안전 | 운영 환경에서 명시적으로 off |

### 결정 필요
- [ ] 옵션 A/B/C 중 선택?
- [ ] C 선택 시 누가 구현? (예상 작업: 1h, `backend/routes/predict.py` + `backend/config.py` + 환경변수 문서화)
- [ ] 시연 환경에서 LightGBM 모델 파일이 실제로 어디 위치하는지 확정 (`ml/models/` 안에 있는지)

---

## 안건 2. P0-6 — HW 미수령 시 "시연 데이터" vs "실측 미검증" 분리 표기

### 현황
- HW 부품 26일 지연, 카메라 실측 0건, AI 카운팅 정확도 표 빈칸 상태.
- 진단서 §6.7: "정확도 리포트는 HW 도착 후 채워야 하는 상태"

### 위험
발표·시연에서 다음 두 가지가 청중에게 혼동될 수 있음:
- **구현 + 검증된** 기능 (광주 BIS 실시간, ML 예측, PWA/Admin UI)
- **구현했으나 미검증** 기능 (Jetson AI 카운팅 25 FPS, DeepSORT 중복 카운팅 방지, Line Crossing 정확도)

### 결정 필요
- [ ] 시연 시나리오를 두 트랙으로 명시 분리?
  - 트랙 A: BIS 실시간 + ML 예측 + UI 흐름 (라이브 데모 가능)
  - 트랙 B: AI 카운팅 (영상 시뮬레이션 또는 "HW 도착 후 검증 예정" 슬라이드)
- [ ] 발표자료 어느 슬라이드에 "미검증" 라벨을 명시할지 (시연 시연계획서 슬라이드?)
- [ ] HW 부품 수령 최종 deadline 결정 (5/14~15 마지노선 → 안 오면 트랙 B 확정 전환)

---

## 안건 3. 적용 누락 보안 — 광주 BIS API 키 회전

### 현황
- 진단서 §6.1 지적: "config.py에 광주 BIS API 키 기본값이 하드코딩되어 있다. 실제 키라면 즉시 회전 대상이다."
- 현재 `backend/config.py:37`에 키 기본값 하드코딩 + GitHub 공개 저장소(`HUNMINRYU/bustago`) push 상태.
- P0-1에서는 **변수명 정합화만** 적용, 키 회전은 미처리.

### 옵션
| 옵션 | 작업 | 리스크 |
|------|------|--------|
| A. 키 재발급 + config.py 기본값 제거 + env 강제 | 공공데이터포털 신청 → 1~3일 소요 | 시연 직전 키 교체 부담 |
| B. 그대로 두기 | 0 | 공공 데이터·호출량 제한 있음 → 악용 가능성 낮음 |
| C. 회의 후 시연 끝나면 재발급 | 시연 후 P1로 처리 | 시연 기간 중 노출 지속 |

### 결정 필요
- [ ] 옵션 A/B/C 선택?
- [ ] A 선택 시 누가 재발급 신청? (공공데이터포털 본인 인증 필요)

---

## 안건 4. P1 — 시연 후 정리할 6건

진단서 §8 P1 항목. **시연 D-8 안에 자율 적용 비추천**, 다만 우선순위는 결정해두는 게 좋음.

| # | 항목 | 진단서 § | 예상 작업 | 담당 후보 |
|---|------|---------|----------|---------|
| P1-a | `ml/data_collection/test_*.py` 실제 API 호출 분리 (pytest 안전성) | 6.6 | 1h | 류훈민 |
| P1-b | backend 테스트를 임시 DB 기반으로 재구성 | 6.6 | 2~3h | 이트겔 |
| P1-c | CI 추가 (lint·unit·docker build) | 6.6 | 2h | 류훈민 |
| P1-d | ML 검증 재설계 (광주 holdout, feature leakage 점검) | 6.5 | 4~6h | 류훈민 |
| P1-e | `/api/crowd-count`에 장치 토큰/서명 추가 | 6.1 | 1~2h | 이트겔 |
| P1-f | broad `except Exception` 제거/로깅 처리 | 6.4 | 2h | 류훈민 |

### 결정 필요
- [ ] 시연 후 작업 순서?
- [ ] P1-d (ML 재검증)는 광주 실측 데이터가 있어야 가능 → HW 도착 후만 가능

---

## 안건 5. P2 — 장기 정리 5건

진단서 §8 P2 항목. 시연 후 + 발표 후로 미룸.

| # | 항목 | 진단서 § |
|---|------|---------|
| P2-a | Frontend `innerHTML` 조립 → DOM API/escaping (XSS 위험 축소) | 6.1 |
| P2-b | Service Worker 캐시 버전 관리 정리 | 6.1 |
| P2-c | schema versioning / 마이그레이션 도입 | 6.3 |
| P2-d | 빈 문서·placeholder 정리 (일부 처리됨: 02_설계 docx 3개) | — |
| P2-e | 운영 로그, request id, metrics 추가 | 6.4 |

### 결정 필요
- [ ] 발표 후 보고서·논문 단계에서 어떤 항목까지 다루나?

---

## 안건 6. 진단서 §10 "추가로 확인해야 할 질문 8개"

진단서가 제기한 본질적 질문들 — 회의에서 한 번에 명확히 답해두면 이후 결정 흐름이 빨라짐.

1. 이 프로젝트의 목표는 "발표 시연"인가, "실제 정류장 운영"인가?
2. 광주대학교 현장 실측 데이터는 언제부터 얼마나 수집 가능한가?
3. 현재 광주 BIS API 키는 실제 운영 키인가, 테스트 키인가?
4. 카메라 촬영에 대한 개인정보·학교 승인 절차는 완료됐는가?
5. 모델 정확도 KPI는 무엇인가? (혼잡도 등급 정확도 / 탑승 가능성 정확도 / 카운팅 오차)
6. 배포 대상은 노트북 / 교내 서버 / 클라우드 / Jetson 자체 중 어디인가?
7. HW 미수령 지속 시 발표에서 "AI 카운팅"을 어떤 근거로 주장할 계획인가?
8. 운영자가 실제로 의사결정할 액션은 증차 / 셔틀 배차 / 안내 문구 중 무엇인가?

---

## 안건 7. YOLO11 학습 로드맵 (deep-research-report 2부 분석 결과)

### 현황
- 5/14 기준 **HW 부품 전체 도착 완료**
- `docs/deep-research-report.md` / `_v2.md` 분석 결과:
  - 이 브랜치는 YOLOv11 **추론 호환은 OK**, **학습 파이프라인은 0%** (dataset YAML·train_yolo.py·counter.py 클래스 일반화·평가 레이어 부재)
  - 보고서 핵심 해석: 프로젝트 KPI(`count_in`/`count_board`/`current_waiting`)는 **사람 흐름**이므로, **버스/차량 검출이 아닌 person fine-tuning이 정답**

### 갱신된 일정 (5/14 ~ 6/04)

| 구간 | 작업 | 담당 후보 |
|------|------|---------|
| 5/14 ~ 5/18 | Stage 0: 사전학습 `yolo11n.pt` 그대로 추론 검증 + counter.py `--classes` 인자 추가 | 류훈민 |
| 5/19 ~ 5/21 | Stage 1: 학습 인프라 코드 (train_yolo.py, bustago_person.yaml, requirements-train.txt) — **학습은 안 함, 코드만** | 류훈민 (Codex 보조) |
| 5/22 ~ 5/26 | Stage 2: 광주대 정류장 영상 수집·라벨링 시작 + **판넬 작성 병렬** | 박건우(수집) + 이건영(라벨링) |
| 5/27 ~ 6/02 | Stage 3: person fine-tuning (`yolo11n → yolo11s`, imgsz=960, epochs=100) | 류훈민 |
| 6/03 ~ 6/04 | Stage 4: Jetson `best.engine` 배포 + 리허설 + 정확도 리포트 채우기 | 류훈민 + 이트겔 |

### 결정 필요
- [ ] **학습 대상 확정: A(사람 — 권장) / B(버스·차량) / C(둘 다)** ← 보고서는 A 강력 권장
- [ ] **학습 환경**: x86 학습 + Jetson export 분리 vs Jetson 직접 학습 ← 보고서는 **분리** 권장 (JetPack 6.x 호환 문제)
- [ ] **데이터 규모**: 1k장 / 2~3k장 / 5k장+ ← 보고서 권장 2~3k장 (현장 수집 + COCO pretrained)
- [ ] **라벨링 도구**: CVAT / Label Studio / Roboflow ← 라이선스·UX 비교 후 결정
- [ ] **개인정보·학교 승인**: 광주대학교 정류장 영상 수집 사전 허가 필요 (보고서 §10 본질질문 4번과 연결)

---

## 안건 8. 6/04 캡스톤디자인 경진대회 일정 + 산출물

### 마일스톤

| 날짜 | 마감 | 산출물 | 담당 |
|------|------|--------|------|
| **2026-05-18** | 경진대회 신청서 | [별지 제1호] 1p — `docs/_회의준비/2026-05-18_경진대회_신청서_초안.md` 활용 | 대표학생 |
| **2026-05-21** | 1차 시연 (당초 D-day) | SW 99% 동작 시연용 | 전원 |
| **2026-05-26** | 캡스톤 판넬 제출 | [별지 제2호] 3p — `docs/_회의준비/2026-05-26_캡스톤_판넬_초안.md` (예정) | 류훈민(작성) + 박건우(편집·인쇄) |
| **2026-06-04** | 경진대회 본행사 | 실물 Jetson + Pi 라이브 데모 + 판넬 거치 | 전원 |

### 신청서 — 결정 필요
- [ ] **팀명**: "같이타요" 유지? (기존 한글 양식 기준)
- [ ] **대표학생** 1명 확정 (이름·연락처)
- [ ] **전기 콘센트** 필요 여부 ☑ "필요" 확정 (Jetson + Pi + 디스플레이 + 노트북)
- [ ] **작품 내용요약** 안 A(10줄) vs 안 B(5줄) 선택 ← 칸 크기 기준

### 판넬 — 결정 필요 (5섹션 5/26 마감)
- [ ] **1. 작품개요·필요성**: 광주대 셔틀·시내버스 만차·혼잡 문제 정의
- [ ] **2. 이론·기술현황**: LightGBM + YOLOv11 + DeepSORT + PWA + 광주 BIS API
- [ ] **3. 개발방법·과정**: 4 Phase + 사용자 여정 + 광주 이식
- [ ] **4. 결과물**: 시연 스크린샷 + AI 카운팅 정확도 + 광주 BIS 통합
- [ ] **5. 기대효과·활용**: 학생 의사결정 지원 + 운영자 증차 판단 + 타 캠퍼스 확장성
- [ ] **참여기업** 항목 → 빈칸/지도교수 협의 후 결정
- [ ] **사진 원본 파일** (jpg) 별도 제출 → 누가 수집·정리?

### 경진대회 당일 데모 — 결정 필요
- [ ] **데모 시나리오**: 트랙 A(BIS 실시간 + ML 예측 + UI) + 트랙 B(AI 카운팅) 분리 표기 (안건 2와 동일)
- [ ] **준비물**: Jetson + Pi + 카메라 + 7인치 디스플레이 + 노트북 + 멀티탭 + 인터넷(테더링/와이파이)
- [ ] **리허설 일정**: 5/30, 6/02 등 2회 권장

---

## 빠른 결정 추천 순서 (5/14 기준 — 마감 가까운 순)

1. **안건 8 - 신청서 (5/18, D-4)** — 대표학생·요약 안 선택 즉시 결정 ★★★
2. 안건 2 (HW 분리 표기) — 발표·판넬에 영향 ★★
3. 안건 1 (모델 fallback) — 시연 전 토글 도입 권장 ★
4. **안건 7 - YOLO11 학습 대상·환경·데이터 규모** — Stage 2 데이터 수집 시작일(5/22) 전에 확정 ★
5. **안건 8 - 판넬 5섹션 분량·이미지** — 5/22~5/26 작성 일정 ★
6. 안건 3 (API 키 회전) — B(현행) or C(시연 후) 결정만
7. 안건 6 (8개 질문) — 가능한 만큼
8. 안건 4·5 (P1·P2 순서) — 시간 남으면

---

## 참조 문서

- 진단서 원본: [../아키텍처_종합진단_2026-05-13.md](../아키텍처_종합진단_2026-05-13.md)
- 최근 STATUS: [../STATUS_2026-05-13.md](../STATUS_2026-05-13.md)
- 적용된 P0 4건 커밋: `c9c7efa`
- **deep-research-report**: [../deep-research-report.md](../deep-research-report.md) / [_v2](../deep-research-report_v2.md)
- **신청서 초안**: [./2026-05-18_경진대회_신청서_초안.md](./2026-05-18_경진대회_신청서_초안.md)
- **판넬 초안** (작성 예정): `./2026-05-26_캡스톤_판넬_초안.md`
